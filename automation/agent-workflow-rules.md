# Правила работы агента в этом репо

Операционные правила для любого агента (Claude, Hermes, Claude Code) — как вести файлы, а не про психологию пользователя (это в `docs/reglament/00-index.md`, LPF-регламент). Новое правило добавляется отдельным пунктом с датой, без вычитки и переформулировки старых при каждом добавлении.

**Любой агент, который создаёт, перезаписывает или удаляет файлы в этом репозитории, обязан прочитать этот файл перед первой такой операцией в сессии.** Если в ходе работы обнаружен новый системный риск/баг инструментов — фиксировать сюда отдельным пунктом с датой. `docs/drr-candidates.md` — не то место: он про паттерны Марии из дневника, не про инструменты и файловые операции.

## 1. Утренняя телеметрия — сразу в файл (с 28.08.2026)

Утренние метрики (сон/энергия/ёмкость) агент-Дневник заносит в `telemetry/history.md` и `telemetry/diary-2026-08.md` сразу по получении от пользователя в чате, не дожидаясь вечера. Остальные поля (обед/ужин/тренировка/алкоголь/триггеры/эмоциональный фон) дозаполняются по ходу дня или на вечернем чекине — значение TBD в них до вечера нормально, это не ошибка агента.

## 5. Анонимный `web_fetch` на GitHub не подтверждает существование/содержимое файла в этом репозитории (с 28.08.2026)

Репозиторий `DS-maria-lpf` приватный. Анонимный (без авторизации) `web_fetch` на `github.com/mgalyamova-commits/DS-maria-lpf/...` вернёт 404 независимо от того, существует файл или нет — это ошибка доступа, не подтверждение отсутствия. Наблюдалось 28.08: `web_fetch` дал 404 на файл, который на тот момент существовал. Никогда не использовать `web_fetch`/`web_search` на GitHub-адреса этого репозитория как способ проверки — только `personal_*` инструменты.

## 6. Собственная ошибка агента: частичная перезапись вместо полной (с 29.08.2026)

Отдельно от рисков стейл-рида инструментов — прямое нарушение агентом собственного известного правила «читать целиком → переписывать целиком». Наблюдалось 29.08: агент-Дневник вызвал `personal_write` по `docs/moto-dnevnik.md`, передав в `content` только новый фрагмент (одну добавляемую секцию) без предварительного свежего чтения всего файла — файл был полностью заменён этим фрагментом, весь остальной контент (несколько разделов, накопленных в этой же сессии) временно потерян. Обнаружено сразу при проверке, восстановлено из последней полной версии, видимой в контексте сессии.

**Правило:** независимо от того, добавляется ли контент («допиши абзац») или переписывается целиком — перед КАЖДЫМ `personal_write` в существующий файл обязательны: (1) свежий `personal_get_document`, (2) сборка полного нового содержимого файла (старое + новое) в `content`, а не только нового фрагмента. Это применимо даже когда правка кажется маленькой и «явно безопасной» — именно на таких мелких правках риск пропустить полный цикл выше всего.

## 7. Репозиторий значительно шире, чем знает README (с 29.08.2026)

Обнаружено при попытке полного бэкапа 29.08: реиндекс показал 31 файл, а README на тот момент документировал только ~19. Недостающий слой — 9 проектных папок в корне (`<slug>/context.md`: helsnet, helsko, motoferma, himozin, gpb-mbs, tandem, openbio, zhivye-sistemy, fondobrazovanie), `startup-studio/` (2 файла), `automation/reminders-routine.md` (конфигурация Telegram-напоминаний). README ошибочно утверждал, что контекст проектов живёт только в Google Docs. `automation/repo-map.md` оказался более актуальным (частично знал про проектные папки), но тоже не полностью синхронизирован с README. Исправлено в README 29.08.2026.

**Правило:** нет отдельного инструмента «показать все файлы репозитория» — `personal_search` не гарантирует полноты для этой цели. Единственный способ получить гарантированно полный список — попросить пользователя проверить через сам GitHub (веб-интерфейс или `git clone` + `find`). Не полагаться на реконструкцию списка файлов через серию поисковых запросов как на исчерпывающую — это даёт «лучшее из доступного», не гарантию полноты.

## 8. Регламент разбит на части — обновить все внутренние ссылки на старый путь (с 29.08.2026)

`docs/lpf-reglament-v2.5.md` больше не единый файл — разбит на 9 частей + оглавление в `docs/reglament/`. Старый путь оставлен редиректом (не удалён), но любая ссылка вида «см. `docs/lpf-reglament-v2.5.md`, раздел N» или «см. `LPF-reglament.md`» — устарела и должна вести на конкретную часть в `docs/reglament/` (нумерация частей — см. `docs/reglament/00-index.md`). При правке любого файла, где встречается такая ссылка — обновлять сразу, не откладывать. Найденные и исправленные 29.08.2026: `README.md`, `automation/repo-map.md`, `docs/otnosheniya-pravila.md`, `prompts/dnevnik-prompt.md`, `docs/drr-candidates.md`.

## 9. Защита от гонки записи через SHA-версионирование (с 30.08.2026)

Техподдержка AIAssystant починила консистентность инструментов на своей стороне (см. «Закрытые пункты» ниже — баги устаревшей выдачи и усечения длинных файлов исправлены) и добавила версионирование для защиты от гонки записи: `personal_get_document` принимает параметр `include_sha: true` и возвращает текущий `sha` файла; `personal_write` принимает `expected_sha` — если файл изменился с момента чтения, запись отклоняется с ошибкой вместо тихого затирания чужих правок.

**Важная оговорка от техподдержки: защита работает только если агент реально передаёт эти параметры.** Без `include_sha`/`expected_sha` поведение прежнее (риск гонки записи как в пункте #3 истории, см. «Закрытые пункты»).

**Обязательное правило для любого агента:** перед `personal_write` в существующий файл — всегда читать через `personal_get_document` с `include_sha: true`, передавать полученный `sha` в `expected_sha` при записи. Если запись отклонена из-за несовпадения sha — перечитать файл заново (уже с учётом чужих изменений), собрать содержимое заново, повторить попытку записи с новым sha.

**Как проверить, что защита реально работает (не просто существует в API) — тест не проводился, будущему агенту сделать при случае:** открыть один и тот же файл в двух параллельных сессиях, в первой прочитать sha и записать без изменений содержимого, во второй (с более старым sha) попытаться записать что угодно — ожидаемый результат: вторая запись должна вернуть ошибку несовпадения версии, а не тихо применить правки. Пока не проверено вживую, полагаться на слова техподдержки, но не считать риск гонки записи закрытым на 100% до реальной проверки.

---

## ПРОЦЕДУРА: Проверка целостности системы (с 29.08.2026)

Запускается после любой структурной операции: разбиение файла на части, массовое переименование, перенос источника истины (например, из Google Docs в репо или наоборот), любая правка, затрагивающая больше одного файла разом.

1. **Реиндекс (если ещё требуется — см. пункт 9, возможно уже не нужен отдельным шагом).** Зафиксировать итоговое число файлов и сверить с ожидаемым (было N, стало N + добавленные − удалённые).
2. **Существование файлов.** По каждому файлу, упомянутому в README, — подтвердить, что он реально существует (`personal_get_document` или `personal_search` по точному пути), не полагаясь на память о том, что «должно быть».
3. **Битые ссылки.** По каждой перекрёстной ссылке в изменённых файлах (например, «см. раздел 8» после разбиения на части) — убедиться, что она указывает на реально существующий путь или раздел, а не в никуда.
4. **Смысловая полнота ключевого файла.** Для главного изменённого документа — явный контрольный список того, что должно остаться (например, для регламента: все 14 Ситуаций по номерам, все разделы по старой нумерации), сверка поштучно, не «пробежала глазами».
5. **Промпты веток — путь и ссылки целы.** Для каждой ветки со своим промптом (сейчас: Дневник) — открыть промпт-файл и пройти по каждой ссылке/пути, на который он ссылается (LPF-регламент, otnosheniya-pravila.md, o-marii-fakty-lyudi.md, neudovletvorennosti.md, лексикон и т.д.): путь существует, ведёт туда, куда написано, не на устаревший или переименованный файл. Отдельно проверить раздел START OF DAY/чтения при старте — именно там чаще всего накапливаются ссылки на несколько файлов, и именно там разрыв незаметен до тех пор, пока ветка не перезапустится с нуля.
6. **Контрольное чтение с чистого листа.** Пройти инструкцию из README так, как если бы это делал новый агент при первом запуске (реально прочитать файлы по цепочке, а не по памяти о том, что там должно быть) — путь не должен обрываться и не должен вести в никуда.
7. **Сравнение со снапшотом (если он есть для этой операции).** Например, для разбиения регламента — сверить, что каждый раздел архивного снапшота (`docs/archive/lpf-reglament-v2.6-snapshot-2026-08-29.md`) нашёл своё место в новой структуре: ничего не потеряно, ничего не задвоено.

**Правило:** процедура прогоняется целиком, не выборочно — пропуск одного пункта («это же мелкая правка») ровно то, из-за чего произошёл инцидент #6 выше.

---

## ПРОЦЕДУРА: Полный бэкап IWE (с 29.08.2026)

Бэкап всего репозитория целиком, вне репозитория — на случай потери самого репо (не спасают снапшоты отдельных файлов внутри него самого).

**Периодичность:** раз в неделю, привязано к ритуалу недельной сводки (см. `docs/reglament/07-propuski-svodka.md`) — та же сессия, заодно; плюс дополнительно перед любой крупной структурной операцией (например, перед разбиением регламента 29.08.2026).

**Способ:** основной способ — пользователь скачивает репозиторий целиком через GitHub («Code» → «Download ZIP») и сохраняет архив сам (например, в Google Drive вручную). Это даёт гарантированно полную и точную копию без риска пропусков, в отличие от попытки агента реконструировать содержимое через `personal_search`/`personal_get_document`.

Агент может дополнительно собрать вспомогательный документ (список файлов, структура, найденные через поиск фрагменты) и сохранить в Google Drive как **дополнение**, явно помеченное «не гарантированно полная копия» — но не как замену ZIP-архиву пользователя.

**Хранение:** пользователь держит несколько последних ZIP-архивов с датой в названии (например, «DS-maria-lpf — 29.08.2026.zip»). Явного лимита нет — это делает пользователь у себя, не агент.

---

## ПРОЦЕДУРА: Восстановление из бэкапа (с 29.08.2026)

**ZIP-архив восстанавливает содержимое файлов на 100% — но не всю архитектуру целиком.** Четыре вещи находятся за пределами архива и требуют отдельных шагов после распаковки:

1. **История коммитов (git-история) не входит в ZIP.** ZIP — это срез файлов на момент времени, не хронология правок. Если репозиторий пересоздаётся из ZIP с нуля (не восстанавливается тот же самый) — история «кто/когда/зачем менял» (сообщения коммитов, sha) теряется безвозвратно. Содержимое файлов цело, история правок — нет. Если возможно физически (репозиторий не уничтожен, а только повреждён/испорчен) — предпочтительнее откатить нужные файлы через git (например, вернуть предыдущий коммит), а не пересоздавать репозиторий с нуля из ZIP, именно чтобы сохранить историю.

2. **Подключение к Aisystant (MCP) нужно переустановить, если репозиторий пересоздан заново.** Новый (не тот же) репозиторий потребует: `personal_connect_source` → дождаться подключения → `personal_reindex_source` → дождаться `succeeded`. Это не потеря данных, а обязательный технический шаг, который легко забыть в стрессовой ситуации восстановления.

3. **Внешние источники в Google Docs не защищены ни ZIP, ни этим бэкапом.** Несколько файлов до сих пор ссылаются на исходники вне репозитория (журнал изменений, журнал отношений, сводные заметки по трекам обучения и другие — см. README). Если инцидент коснётся конкретно репозитория — они не пострадают. Но если проблема на уровне Google-аккаунта — ни ZIP, ни репозиторий их не спасают. Это отдельный риск, не покрытый текущей процедурой бэкапа.

4. **Состояние Telegram-бота с напоминаниями не восстанавливается автоматически.** `automation/reminders-routine.md` описывает конфигурацию слотов и правило продления, но уже запущенные напоминания (последний известный батч: 28.08–03.09.2026) — это состояние самого бота (@aist_me_bot), не файла. Восстановление файла из бэкапа не восстанавливает автоматически ранее поставленные напоминания — после любого восстановления нужно явно попросить агента «продлить напоминалки» заново по инструкции из файла.

**Правило:** после каждого крупного бэкапа — рекомендуется тестовое восстановление (не дожидаясь реальной катастрофы): взять актуальный ZIP, попробовать реально пройти шаги 1-4 выше в тестовом виде хотя бы раз, чтобы процедура была проверена на практике, а не оставалась предположением «наверное, сработает».

---

## Кандидаты (ждут подтверждения, не применены как обязательные правила)

- **[28.08.2026] Общее правило для всех веток: при создании нового файла — README должен обновляться в той же операции.** Сейчас README регулярно расходится с фактической структурой репо, потому что обновление README не было обязательным шагом при создании файла. Кандидат в правило: любой агент, создающий новый файл в этом репозитории, обязан в той же сессии добавить на него ссылку в README (в подходящий раздел) — не откладывать на «потом обновлю».
- **[29.08.2026] «Журнал изменений» и «Отношения — журнал» существуют только в Google Docs, без локальной копии.** Журнал изменений — вспомогательный технический журнал (структурные сдвиги, недельные итоги), Отношения — журнал — сырой поток расшифрованных заметок, обрабатываемый в `docs/otnosheniya-pravila.md` и `docs/o-marii-fakty-lyudi.md`. Открытый вопрос: нужна ли локальная копия для этих двух, или они органично остаются сырым внешним потоком, который не читается агентами напрямую каждую сессию (в отличие от портянки/регламента/правил).

## Закрытые пункты (для истории)

- ~~`automation/repo-map.md` устарел, ссылается на несуществующий `LPF-reglament.md`~~ — исправлено 29.08.2026, манифест полностью переработан.
- ~~Портянка пишется в Google Docs, хотя есть локальная копия~~ — уточнено Марией 29.08.2026: миграция уже завершена, `docs/portyanka-obshiy-stek.md` — источник истины. Регламент (часть 3) обновлён соответственно.
- ~~Пункт 2: `personal_search` систематически не находит существующие файлы репо из-за отставания индекса~~ — исправлено техподдержкой AIAssystant 30.08.2026 (сообщение техподдержки: «новый или изменённый файл появляется в поиске сам, через несколько секунд после записи, без всяких форс-реиндексов»). Форс-реиндекс перед проверкой существования файла больше не требуется как обязательный шаг.
- ~~Пункт 3 (обходной путь): риск гонки записи между параллельными ветками~~ — сама проблема (несколько веток пишут в один файл параллельно) не исчезла, но обходной путь «форс-реиндекс + чтение перед записью» заменён на правило #9 (SHA-версионирование), более надёжный механизм на стороне сервиса.
- ~~Пункт 4: `personal_get_document` возвращает устаревший/неполный контент, включая усечение больших файлов~~ — исправлено техподдержкой AIAssystant 30.08.2026 (усечение длинных файлов исправлено накануне, 29.08.2026; консистентность выдачи — 30.08.2026). Регламент с 14 Ситуациями и другие длинные файлы читаются целиком без разбиения на чанки индекса.
